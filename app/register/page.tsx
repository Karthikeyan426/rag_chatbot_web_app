import RegisterForm from "@/components/auth/RegisterForm";

export default function RegisterPage() {
    return (
        <main className="flex w-full h-full items-center justify-center bg-background px-4 bg-gray-50">
            <RegisterForm />
        </main>
    );
}