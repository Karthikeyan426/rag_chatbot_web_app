import LoginForm from "@/components/auth/LoginForm";

export default function LoginPage() {
    return (
        <main className="w-full h-full flex items-center justify-center bg-background bg-gray-50">
            <LoginForm />
        </main>
    );
}
